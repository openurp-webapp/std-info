import {
  equals22
} from '../../../helpers/equals22';
import { module, test } from 'qunit';

module('Equals22Helper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = equals22(42);
  assert.ok(result);
});
