import {
  ifEquals
} from '../../../helpers/if-equals';
import { module, test } from 'qunit';

module('IfEqualsHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = ifEquals(42);
  assert.ok(result);
});
